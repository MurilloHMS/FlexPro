version: '3.8'

services:
  flexpro:
    build:
      context: . 
      dockerfile: Dockerfile 
    ports:
      - "80:8080"  
      - "81:8081"
    environment:
      ASPNETCORE_ENVIRONMENT: Development  
    restart: always  
    networks:
      - flexpro-network

networks:
  flexpro-network:
    external: true

